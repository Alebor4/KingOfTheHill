using UnityEngine;

// TestComponent.cs
// Простой компонент для проверки компиляции: если он виден в Add Component — компилятор работает.
public class TestComponent : MonoBehaviour
{
    public int testValue = 1;
}
