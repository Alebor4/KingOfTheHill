// TileTypes.cs
// Объявление enum в глобальной области видимости — решает ошибку "TileType not found".
public enum TileType
{
    Red = 0,
    Blue = 1,
    Green = 2,
    Yellow = 3,
    Purple = 4
}
